# GeneticAdditiveModels

``main.py`` holds the basic GA code to train the structure of the GAM but this results in subpar performance. This could be because there are better ways to tune hyper parameters like knots and ```math $\lambda$``` refinement.

``enhanced.py`` holds this instead.